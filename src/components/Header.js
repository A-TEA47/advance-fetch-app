import React from 'react';
import '../styles/Header.css';  // Ensure this path is correct

const Header = () => {
  return (
    <header>
      <h1>Fetch API App</h1>
    </header>
  );
}

export default Header;
